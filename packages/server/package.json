{
  "name": "@openauthcert/server",
  "version": "0.0.0",
  "description": "Fastify badge server — issue, revoke, and verify OpenAuthCert badges.",
  "type": "module",
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/rate-limit": "^11.1.0",
    "@openauthcert/core": "workspace:*",
    "fastify": "5.10.0"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2",
    "vitest": "4.1.10"
  }
}
