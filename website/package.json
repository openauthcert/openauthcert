{
  "name": "openauthcert-site",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-key": "node scripts/sync-public-key.mjs",
    "gen-badges": "node scripts/generate-badges.mjs",
    "prep": "pnpm sync-key && pnpm gen-badges",
    "dev": "pnpm prep && vitepress dev docs",
    "build": "pnpm prep && vitepress build docs",
    "serve": "vitepress serve docs"
  },
  "dependencies": {
    "@openauthcert/core": "workspace:*",
    "vitepress": "1.6.4"
  },
  "devDependencies": {
    "typescript": "6.0.3",
    "vue": "3.5.35"
  }
}
